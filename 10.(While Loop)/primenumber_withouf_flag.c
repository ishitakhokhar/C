// Find whether the given number is prime or not .
#include <stdio.h>
void main()
{
    int num, n, n, i;
    printf("Enter number : ");
    scanf("%d", &num);
    while (i <= num / 2)
    {
        if (num % i == 0)
    }