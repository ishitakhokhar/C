// Count numbers higher than the average of an array.
#include <stdio.h>
void main()
{
    int n, i, sum = 0, count = 0;
    float avg;
    printf("Enter number : ");
    scanf("%d", &n);
    int arr[i];
}
